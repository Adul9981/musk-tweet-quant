# 数据来源与模型局限

> 使用任何规律前，先了解它是在什么条件下成立的，在什么条件下可能失效。

---

## 一、数据来源

| 项目 | 数据 |
|------|------|
| **数据量** | 206天 · 8942条推文 · 4921个小时数据点 |
| **覆盖范围** | 2025年11月14日 — 2026年5月25日（约6个月） |
| **数据来源** | xtracker.polymarket.com 历史追踪数据 |
| **日均推文** | 43.4条/天 |
| **最后更新** | 2026-05-25 |
| **回测期数** | 33个追踪市场期次 |

---

## 二、µ的不确定性（随剩余时间变化）

这是最重要的局限性表格。剩余时间越长，µ误差越大。

| 剩余时间 | µ的不确定性（约1σ） | 含义 |
|---------|-----------------|------|
| 3天 | ±50条 | 区间判断不可靠，**不要入场** |
| 2天 | ±35条 | 开始有参考价值 |
| 1天 | ±20条 | 较可靠，**适合入场** |
| 12小时 | ±12条 | 高可信，可加仓 |
| 6小时 | ±8条 | 非常可信 |

> **🔴 铁律**：到期前超过3天不入场，µ误差太大，不是投资，是赌博。

---

## 三、模型局限性

### 3.1 泊松模型的局限

- 马斯克发推有**突发性（肥尾效应）**，泊松模型低估极端事件
- 真实概率在极端区间（<5%）可能略高于模型预测
- **实际操作影响**：高赔率小仓（§10.5）有一定合理性，但不是借口大仓押注

### 3.2 模型系统性偏差

- **33期回测显示模型存在 +0.3档的系统性偏高**
- 意思是：模型预测的中心落点，实际上比真实结果平均偏高约0.3个区间
- **实际操作影响**：保护仓应放在主仓的**下方**1档，而非上方

### 3.3 会话模型的适用范围

- 会话模型基于205天673个会话数据
- 对"今日会话是否发生"有概率预测，但不能精确预测每小时的推文数
- 会话缺席的µ修正（下移约11–14条）是统计均值，单日偏差可能更大

---

## 四、历史数据的重要修正

> ⚠️ **之前基于5天数据的分析结论（BJ 08:00为峰值）已被推翻。**
> 6个月206天的数据揭示了完全不同的真实规律。
> **马斯克是真正的美国夜猫子，峰值在CDT凌晨1点（BJ 14:00）。**

- BJ 08-12（美国傍晚7-11pm）是**低谷**，不是高峰
- 旧版规则中"傍晚高峰"的概念已过时，不要引用

---

## 五、数据更新

| 追踪数据区间 | 追踪数量 | 天数 | 更新时间 |
|------------|---------|------|---------|
| 2025-11-14 至 2026-05-25 | 33个追踪 | 206天 | 2026-05-25 |

建议每月末运行 `/tmp/fetch_musk_patterns.py` 脚本更新行为分析数据。

---

## 六、推文数据源清单（按优先级排序）

> ⚠️ xtracker.polymarket.com 存在间歇性 bug（计数卡住、返回空数组），需多源备份。

| 优先级 | 数据源 | 接入方式 | 状态 | 备注 |
|--------|--------|---------|------|------|
| 🥇 主用 | **xtracker.polymarket.com** | `/api/xtracker`（Vercel 代理） | 间歇性故障 | 与 Polymarket 规则 100% 一致，但会卡住 |
| 🥈 备用1 | **elon-tracker.com Supabase** | 直接读公开 Supabase API | ✅ 可用 | 见下方详细说明 |
| 🥉 备用2 | **twitterapi.io** | `/api/elon-tweets`（Vercel API） | ✅ 验证可用 | Key: `new1_8452e6aed9cd49e9b163a11635102474`，实时数据，字段齐全 |
| 4 | **RapidAPI xscraper** | `/api/elon-tweets`（原有） | 不稳定 | Key 在 `.env` / Vercel 环境变量 |
| 5 | **polystrike.xyz** | 社区备用计数器 | 参考用 | 无开放 JSON API，仅供人工核对 |

---

### twitterapi.io（备用2 详细参数）⭐ 推荐下次优先切换

**验证时间**：2026-06-02，测试通过，数据实时

```
端点：  GET https://api.twitterapi.io/twitter/user/last_tweets?userName=elonmusk
Header: X-API-Key: new1_8452e6aed9cd49e9b163a11635102474
```

**返回字段（关键）**：
| 字段 | 说明 |
|------|------|
| `type` | `"tweet"` / `"retweet"` / `"quote"` |
| `isReply` | `true/false` — **过滤回复用此字段** |
| `inReplyToId` | 被回复推文 ID，双重验证 |
| `createdAt` | 发推时间，格式 `"Tue Jun 02 04:41:03 +0000 2026"` |
| `id` | 推文 ID |
| `text` | 推文内容 |

**Polymarket 计数规则**：主帖 + Quote + Retweet，**排除 Reply**
→ 过滤条件：`isReply === false` 即可完全对齐 Polymarket 规则

**分页**：返回 20 条/次，支持 `cursor` 参数翻页向前追溯

**切换步骤**（待执行）：
1. 修改 `/api/elon-tweets.js`，主请求改为 twitterapi.io `last_tweets` 接口
2. 循环翻页抓取当期（7天）所有推文，`isReply=false` 过滤
3. 按日期+小时聚合，返回格式与现有前端兼容（`{date, hour, count}`）
4. 原 RapidAPI 逻辑作为最后 fallback 保留

---

### elon-tracker.com Supabase 数据源（备用1 详细参数）

**发现时间**：2026-06-02，通过逆向 JS bundle 获取

```
Supabase URL:  https://zgqacdzgjdwfuipbsdbr.supabase.co
Anon Key:      eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpncWFjZHpnamR3ZnVpcGJzZGJyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTYxMzMyMTcsImV4cCI6MjA3MTcwOTIxN30._sFDUGL_Lifr84oZ0nWPx26Pf29ol-mGBdMh-mgY-L8
```

**可用表**：
- `elon_tweets` — 每条推文记录，字段：`tweet_id`, `tweet_text`, `tweet_url`, `posted_at`, `reply_count`, `retweet_count`, `like_count`, `processed`
- `tweet_counts` — 汇总计数表（字段待确认）

**调用示例**（直接 fetch，无需后端代理）：
```js
const res = await fetch(
  'https://zgqacdzgjdwfuipbsdbr.supabase.co/rest/v1/elon_tweets' +
  '?order=posted_at.desc&limit=100' +
  '&posted_at=gte.2026-05-26T00:00:00',
  {
    headers: {
      'apikey': 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
      'Authorization': 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
    }
  }
)
```

**注意事项**：
- 数据截至 2025-11-13，**当前（2026年）暂无更新**，说明他们已切换到其他实时源
- 适合作为历史数据补充或验证，不适合作为当期实时计数
- Anon key 是公开前端 key，只读权限，elon_tweets 表对外开放

---

*来源：MUSK_BEHAVIOR_ANALYSIS.md + RULES.md §2 · 最后更新：2026-06-02*
